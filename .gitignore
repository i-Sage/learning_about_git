# ?  ignore all files ending in ".o" or ".a", object and 
# archive files (no .o or .a files)
*.[oa]

# ? but tract lib.a, even though your're ignoring .a files
!lib.a

# ? ignore all files ending with a tilde ~
*~

# ? only ignore the TODO file in the current directory, not
# subdir/TODO
/TODO

# ? ignore all files in the build/ directory
build/

# ? ignore doc/notes.txt, but not doc/server/arch.txt
doc/*.txt

# ? ignore all .pdf files in the doc/ directory
doc/**/*/.pdf